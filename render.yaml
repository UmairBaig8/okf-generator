services:
  - type: web
    name: okf-dashboard
    runtime: docker
    repo: https://github.com/UmairBaig8/okf-generator
    dockerfilePath: Dockerfile.render
    envVars:
      - key: OKF_BUNDLE_DIR
        value: /app/okf_bundle
